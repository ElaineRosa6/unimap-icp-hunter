# Binaries
*.exe
*.dll
*.so
*.dylib
bin/
dist/
output/
/unimap-api
/icp-hunter
/unimap-gui
/unimap-web

# Configs with secrets
configs/config.yaml
.env
# SMTP relay local credentials (SMTP authorization code / password)
smtp-relay/.env
# QQ mailbox password (source file for SMTP setup) — never commit
qqemailpasswd.txt
# smtp-relay deployment/ops scripts (cloud paths, task UUIDs) — deployed via scp
smtp-relay/profile_*.py
smtp-relay/test_send*.py
smtp-relay/fix_admin_token.py
smtp-relay/setup_admin_token.py
smtp-relay/update_channel_header.py
smtp-relay/probe_tasks.py
smtp-relay/bind_email_agent.py
smtp-relay/unbind_wecom.py
smtp-relay/list_channels.py
smtp-relay/dump_queries.py
smtp-relay/trigger_verify.py
# 探测 ICP sidecar 的公司名清单脚本（真实目标指纹）— 不入库
smtp-relay/probe_icp_companies.py
# 云端 ICP 任务创建/探测脚本（容器路径、任务 UUID、公司名）— 不入库
smtp-relay/icp_*.py
# cloud ops scripts with server task UUIDs / container paths
scripts/cloud_quake_stagger.py

# Live monitoring target queries (target intel — local/server only, never commit)
tools/cloud-config-gen/ynmobile_targets.json
# Cloud scheduler task queries with real target fingerprints — local ops reference only
docs/CLOUD_SCHEDULER_TASKS_*.local.md
# Server-side ops scripts that embed target queries, server task UUIDs, or
# container paths — deployed via scp, kept out of the public repo
tools/cloud-config-gen/update_tasks_v2.py
tools/cloud-config-gen/update_quake_weekly.py
tools/cloud-config-gen/hunter_history.py
tools/cloud-config-gen/list_hunter_tasks.py
tools/cloud-config-gen/get_hunter_quota.py

# Logs
*.log
logs/

# Go
vendor/

# Test coverage
*.out
coverage.html
coverage_web.dat

# OS/IDE
.DS_Store
.vscode/
.idea/
*.swp
*~

# IDE tools
.trae/
.venv/

# Docker
docker-compose.override.yml

# Temporary files
tmp/
*.tmp

# Export files (for testing)
*.xlsx
# *.json  # Commented out to allow examples/*.json
performance_benchmark.go

# Debug / test-capture output (engine collection snapshots)
test_hunter*.json
test_*_verify*.json
*_verify.json
*_verify2.json
web_cover

# Credentials
cookies.txt
cookie.txt
unimap_session_cookie.txt
# Audit output (may contain generated secrets, e.g. first-admin password)
.audit-results/
# Incremental audit output (may capture live config secrets) — never commit
.audit-results-incremental/

# Browser "Save Page As" snapshots (html + paired _files/ dir)
132.232.231.41.html
132.232.231.41_files/
port=_*_*.html
port=_*_files/
测试*.html
测试*_files/
360网络空间测绘*.html
360网络空间测绘*_files/
网络空间测绘*.html
网络空间测绘*_files/
*_files/

# Archived historical documents
archive/

# Runtime data
hash_store/**
!hash_store/
!hash_store/.gitkeep
screenshots/**
!screenshots/
!screenshots/.gitkeep
internal/tamper/hash_store/**/*.json
!internal/tamper/hash_store/.gitkeep
internal/tamper/hash_store/records/**/*.json
!internal/tamper/hash_store/records/.gitkeep

# Python scripts
urlive.py

# Claude memory
memory/

# Claude Code local settings (contains permission tokens — never commit)
.claude/settings.local.json

# Runtime data
data/

# Scheduled-task query statements (local rebuild reference — not for VCS)
docs/CLOUD_SCHEDULER_TASKS_*.json
docs/SCHEDULED_TASKS_*.md
# Rewrite rules / query statements containing real target fingerprints and a
# 2026-08-07 history-rewrite token — local ops reference only, never commit
docs/unimap_rewrite_rules*.txt
docs/查询语句*.txt

# Tamper test/runtime data
internal/tamper/data/

# Backup test files
internal/backup/backups/*.tar.gz
web/backups/

# Node.js
node_modules/
package-lock.json

# Project internal audit, logs, and development reports (Local Only)
/reports/
/docs_archive/
/docs/test_reports/
/docs/API_KEYS.md
/docs/AUDIT_REPORT_*.md
/docs/CODE_REVIEW_REPORT_*.md
/docs/DEEP_AUDIT_*.md
/docs/DEEP_REVIEW_*.md
/docs/FIX_REPORT_*.md
/docs/FIX_VERIFICATION_AUDIT_*.md
/docs/OPTIMIZATION_REMEDIATION_PLAN_*.md
/docs/PLAN_*.md
/docs/REVIEW_NOTIFY_FOFA_*.md
web/hash_store/check_records.db

# Go build cache
.gocache/

# CDP login/verification tools (local only)
tools/cdp-login/

# Runtime artifacts (large / regenerated — not for version control)
web/screenshots/
__pycache__/
*.pyc
artifacts/

# Local communication / runtime workspace data (email exports, MCP runtime workspace)
# Root-anchored so subdirectory test fixtures stay visible to VCS
/*.eml
/mimoclaw_workspace/

# Local one-off verification output
/tmp_*

# Reproducible extension regression dependencies in CI
!/tools/extension-screenshot/package-lock.json
